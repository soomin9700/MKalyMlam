# TODO - Implémentation SessionTruck + Gestion des statuts Commande
# Date : 28/06/2026

---

## 1. Création de l'entité SessionTruck
**Fichier :** `src/main/java/com/mkalymlam/entity/SessionTruck.java`

- [x] Nouvelle entité JPA mappant la table `sessionTruck`
- [x] Champs : idSession, idTruck, idItineraire, dateSession, fondDeCaisseOuverture,
        fondDeCaisseCloture, chiffreAffaireTotal, commissionTotaleEquipe, idStatutSession
- [x] Constructeur vide, getters/setters

---

## 2. Création de l'enum StatutCommande
**Fichier :** `src/main/java/com/mkalymlam/entity/StatutCommande.java`

- [x] Enum java avec les valeurs :
  - `EN_ATTENTE`
  - `PREPARATION`
  - `PRETE_POUR_RECUPERATION`
  - `LIVREE`
  - `ANNULEE`

---

## 3. Création de l'enum TypeCommande
**Fichier :** `src/main/java/com/mkalymlam/entity/TypeCommande.java`

- [x] Enum java avec les valeurs :
  - `SUR_PLACE`
  - `A_DISTANCE`

---

## 4. Modification de l'entité Commande
**Fichier :** `src/main/java/com/mkalymlam/entity/Commande.java`

- [x] Remplacé `idSession` (Long) par `@ManyToOne SessionTruck sessionTruck`
  - `@JoinColumn(name = "\"idSession\"")` pointe vers `sessionTruck.idSession`
- [x] Remplacé `idTypeCommande` (Long) par `@Enumerated(EnumType.STRING) TypeCommande typeCommande`
- [x] Remplacé `idStatutCommande` (LocalDateTime, bugué) par `@Enumerated(EnumType.STRING) StatutCommande statutCommande`
- [x] Corrigé `lieuRecuperationPrevu` : `LocalDateTime` → `String`
- [x] Supprimé les getters/setters des anciens champs
- [x] Ajouté les nouveaux getters/setters

---

## 5. Création du SessionTruckRepository
**Fichier :** `src/main/java/com/mkalymlam/repository/SessionTruckRepository.java`

- [x] Interface JPA Repository
- [x] Méthode : `findTopByOrderByIdSessionDesc()` → récupérer la dernière session ouverte

---

## 6. Création du CommandeService
**Fichier :** `src/main/java/com/mkalymlam/service/CommandeService.java`

- [x] Service avec méthode `changerStatut(Long idCommande, StatutCommande nouveauStatut)`
  - Récupère la commande par son ID
  - Change le statut
  - Sauvegarde et retourne la commande mise à jour

---

## 7. Modification de VenteService
**Fichier :** `src/main/java/com/mkalymlam/service/VenteService.java`

- [x] Injection de `SessionTruckRepository`
- [x] Dans `ajouterCommande()` :
  - Initialise `statutCommande` à `EN_ATTENTE` par défaut
  - Associe automatiquement la commande à la dernière session (`findTopByOrderByIdSessionDesc()`)
- [x] Ajout de `getAllCommandes()` pour lister toutes les commandes

---

## 8. Modification de CommandeController
**Fichier :** `src/main/java/com/mkalymlam/controller/CommandeController.java`

- [x] Injection de `CommandeService`
- [x] Nouvel endpoint `POST /commande/changerStatut`
  - Paramètres : `id_commande` (Long), `nouveau_statut` (String)
  - Convertit le string en `StatutCommande` via `valueOf()`
  - Appelle `commandeService.changerStatut()`
- [x] Nouvel endpoint `GET /commande/liste`
  - Retourne toutes les commandes (pour le tableau)

---

## 9. Mise à jour du schéma SQL
**Fichier :** `src/main/resources/schema.sql`

- [x] Table `commande` :
  - `idTypeCommande INT` → `typeCommande VARCHAR(50)`
  - `idStatutCommande INT` → `statutCommande VARCHAR(50)`
  - Supprimé les FKs vers `typeCommande` et `statutCommande`

---

## 10. Mise à jour de l'interface vendeuse (vendeuse.jsp)
**Fichier :** `src/main/webapp/WEB-INF/views/vente/vendeuse.jsp`

### Ajouts :

- [x] **Boutons radio pour le type de commande** (SUR_PLACE / A_DISTANCE)
  - Situé entre le numéro de commande et le tableau des produits
  - Sélectionné avant la création de la commande
  - Envoyé dans le body JSON de `POST /commande/ajouter`

- [x] **Tableau des commandes** (section "Liste des commandes")
  - Colonnes : N°, Date, Type, Montant, Statut, Action
  - Chaque ligne est cliquable → charge la commande dans le panier
  - La ligne sélectionnée est mise en surbrillance

- [x] **Dropdown de changement de statut** par ligne
  - `<select>` avec 5 options de statut
  - Bouton "OK" pour confirmer le changement
  - Appelle `POST /commande/changerStatut`

- [x] **Badge de statut** coloré :
  - EN_ATTENTE → jaune
  - PREPARATION → bleu
  - PRETE_POUR_RECUPERATION → vert clair
  - LIVREE → vert foncé
  - ANNULEE → rouge

- [x] **Badge de type** coloré :
  - SUR_PLACE → violet clair
  - A_DISTANCE → violet foncé

- [x] **Filtre par statut** en haut du tableau
  - `<select>` avec les options : Toutes, EN_ATTENTE, PREPARATION, LIVREE (et tous les statuts)
  - Filtre en temps réel via JavaScript

---

