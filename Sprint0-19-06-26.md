### # CP : Initialisation et Gouvernance

* **Fichiers / Actions à créer :**
1. Génération du squelette sur `start.spring.io` (Dépendances : *Spring Web, Spring Data JPA, Driver BDD (PostgreSQL/MySQL), Lombok*).
2. Création du dépôt **Git** (GitHub/GitLab) et ajout de l'équipe.
3. Création du fichier global `src/main/resources/application.properties` (Configuration des accès à la BDD commune).
4. Création de la structure de packages globale : `com.MkalyMlam.modules.(ingredient, produit, recette, itineraire, vente, statistique)`.


* **Durée :** 1h30 (Priorité immédiate ce matin).

---

### # Fabrice : Module Ingrédients

* **Liste des fichiers Spring Boot à créer :**
1. `Ingredient.java` (Entité avec `@Entity`, `@Id`, `nom`, `quantite`, `prixAchat`)
2. `IngredientRepository.java` (Interface héritant de `JpaRepository<Ingredient, Long>`)
3. `IngredientService.java` (Méthodes métier pour save, delete, findAll, find, update)
4. `IngredientController.java` (Annotations `@RestController` et `@RequestMapping("/ingredient")` avec les méthodes associées)
   1. `/ingredient/save`
   2. `/ingredient/update`
   3. `/ingredient/delete`
   4. `/ingredient/find`
   5. `/ingredient/findAll`


* **Affichage minimal (Validation fonctionnelle) :**
* Un tableau HTML brut (`<table>`) listant tous les ingrédients.
* Un formulaire simple avec des champs textes (`<input>`) et un bouton "Ajouter" pour créer un ingrédient.
* Un lien "Supprimer" sur chaque ligne pour tester la suppression.


* **Durée estimée :** 2h

* **DEADLINE :** Samedi 20h


**Remarques:**
- Creer des donnees en dures dans la base pour les tables avec id foreign key (pour tester)
---

### # Fabrice : Module Produits

* **Liste des fichiers Spring Boot à créer :**
1. `Produit.java` (Entité avec `id`, `nom`, `prixVente`, `categorie`)
2. `ProduitRepository.java` (Interface héritant de `JpaRepository<Produit, Long>`)
3. `ProduitService.java` (Méthodes métier pour save, delete, findAll, find, update)
4. `ProduitController.java` (Annotations `@RestController` et `@RequestMapping("/produit")` avec les méthodes associées)
   1. `/produit/save`
   2. `/produit/update`
   3. `/produit/delete`
   4. `/produit/find`
   5. `/produit/findAll`


* **Affichage minimal (Validation fonctionnelle) :**
* Une page HTML brute avec un tableau listant les produits de votre catalogue de Fast-Food.
* Un formulaire basique d'ajout/modification avec boutons de soumission.


* **Durée estimée :** 2h


* **DEADLINE :** Samedi 20h


**Remarques:**
- Creer des donnees en dures dans la base pour les tables avec id foreign key (pour tester)
---

### # Loïc : Module Recette de Base

* **Liste des fichiers Spring Boot à créer :**
1. `RecetteBase.java` (Entité liant un `id_produit` à un `id_ingredient` avec un champ `quantite_necessaire`)
1. `RecetteBase.java` (Entité avec `@Entity`, `@Id`, `nom`, `quantite`, `prixAchat`)
2. `RecetteBaseRepository.java` (Interface héritant de `JpaRepository<RecetteBase, Long>`)
3. `RecetteBaseService.java` (Méthodes métier pour save, delete, findAll, find, update)
4. `RecetteBaseController.java` (Annotations `@RestController` et `@RequestMapping("/recetteBase")` avec les méthodes associées)
   1. `/recetteBase/save`
   2. `/recetteBase/update`
   3. `/recetteBase/delete`
   4. `/recetteBase/find`
   5. `/recetteBase/findAll`



* **Affichage minimal (Validation fonctionnelle) :**
* Un menu déroulant HTML (`<select>`) pour choisir un Produit, un deuxième pour choisir un Ingrédient, un champ pour la quantité, et un bouton "Lier" pour créer la formule de recette.


**Remarques:**
- Creer des donnees en dures dans la base pour les tables avec id foreign key (pour tester)

* **Durée estimée :** 2h .

* **DEADLINE :** Samedi 20h


**Remarques:**
- Creer des donnees en dures dans la base pour les tables avec id foreign key (pour tester)
---

### # Loïc : Module Itinéraires

* **Liste des fichiers Spring Boot à créer :**
1. `Itineraire.java` (Entité avec `id`, `nomZone` (Analakely, Ivandry, etc.), `heureDebut`, `heureFin`)
2. `ItineraireRepository.java` (Interface héritant de `JpaRepository<Itineraire, Long>`)
3. `ItineraireService.java` (Méthodes métier pour save, delete, findAll, find, update)
4. `ItineraireController.java` (Annotations `@RestController` et `@RequestMapping("/itineraire")` avec les méthodes associées)
   1. `/itineraire/save`
   2. `/itineraire/update`
   3. `/itineraire/delete`
   4. `/itineraire/find`
   5. `/itineraire/findAll`


* **Affichage minimal (Validation fonctionnelle) :**
* Une liste à puces HTML (`<ul>` / `<li>`) répertoriant les différentes zones d'Antananarivo planifiées pour le Food Truck avec un petit bouton de modification en texte brut à côté.


* **Durée estimée :** 2h


* **DEADLINE :** Samedi 20h


**Remarques:**
- Creer des donnees en dures dans la base pour les tables avec id foreign key (pour tester)
---

### # Fifa : Module Saisie de Vente et Commande

* **Liste des fichiers Spring Boot à créer :**
1. `Commande.java` (Entité avec `id`, `date`, `montantTotal`)
2. `LigneCommande.java` (Entité avec `id`, `id_commande`, `id_produit`, `quantite`, `sousTotal`)
3. `CommandeRepository.java` & `LigneCommandeRepository.java`
4. `VenteService.java` (Logique critique : doit calculer le `montantTotal` en sommant les lignes )
5. `CommandeController.java` & `LigneCommandeController.java` (Endpoints `/commande/ajouter`, `/ligneCommande/ajouter`, `/ligneCommande/montant`, `/commande/montant`)
page minimal (Validation fonctionnelle) :**
* **L'interface Vendeuse brute :** Un bouton "Nouvelle Commande". Une fois cliqué, un tableau s'affiche où l'on peut ajouter des lignes (sélection d'un produit et quantité => montant(auto-calculé)).
* Un texte brut en bas affichant en temps réel : `Montant Total : XX Ar` calculé par votre route `/commande/montant`.

* **Affic


* **Durée estimée :** 4h

* **DEADLINE :** Samedi 20h



**Remarques:**
- Creer des donnees en dures dans la base pour les tables avec id foreign key (pour tester)
---

### # Hasina : Module Dashboard Statistique

* **Liste des fichiers Spring Boot à créer :**
1. `StatistiqueController.java` (`/chiffreAffaire`, `/benefice`, `/benefice/idItineraire/`, etc.): a partir de commande et itineraires
2. `StatistiqueService.java` (Contient les requêtes d'agrégation SQL natives comme : `SELECT SUM(c.montantTotal) FROM Commande c`)


* **Affichage minimal (Validation fonctionnelle) :**
* **Dashboard brut :** De simples balises de titre (`<h1>` et `<h2>`) affichant les grands indicateurs :
* "Chiffre d'Affaire Global : [Valeur récupérée de l'API] Ar"
* "Bénéfices totaux : [Valeur] Ar"


* Utiliser graphe : intégrer avec chart deja existant (efa hananantsika)



**Remarques:**
- Creer des donnees en dures dans la base pour les tables avec id foreign key (pour tester)

* **Durée estimée :** 4h.


* **DEADLINE :** Samedi 20h


**Remarques:**
- Creer des donnees en dures dans la base pour les tables avec id foreign key (pour tester)




Voici le module complet d'**Aro** rédigé exactement selon le même modèle, avec les URL d'API, les fichiers à créer, l'affichage minimaliste et la durée estimée.

---

### # Aro : Module Authentification (Login / S'inscrire)

#### Table: utilisateur

* **Endpoints API REST à créer :**
* S'inscrire (Créer un compte) : `/auth/inscription`
* Se connecter (Authentification) : `/auth/connexion`
* Récupérer le profil actuel (Pour vérification) : `/auth/profil`


* **Liste des fichiers Spring Boot à créer :**
1. `Utilisateur.java` (Entité avec `@Entity`, `@Id`, `username`, `password`, `role` [ex: ADMIN, VENDEUSE])
2. `UtilisateurRepository.java` (Interface héritant de `JpaRepository<Utilisateur, Long>`)
3. `AuthentificationService.java` (Logique de hachage/vérification des mots de passe et validation des identifiants)
4. `AuthentificationController.java` (Annotations `@RestController` et `@RequestMapping("/auth")`)


* **Affichage minimal (Validation fonctionnelle) :**
* **Page d'inscription :** Un formulaire HTML brut avec des champs pour l'identifiant (`username`), le mot de passe (`password`), un menu déroulant pour le rôle, et un bouton "S'inscrire".
* **Page de connexion (Login) :** Un formulaire avec deux champs (`username` et `password`) et un bouton "Se connecter".
* **Indicateur visuel de succès :** Après soumission, un simple texte s'affiche à l'écran : `"Bienvenue [Nom d'utilisateur] !"` (si connecté) ou `"Identifiants incorrects"` (en cas d'échec) pour prouver que l'API renvoie la bonne information.


* **Durée estimée :** 3h30

* **DEADLINE :** Samedi 20h


### # Giovan,Nampoina,Ismael : Integration dans template

* **Liste des fichiers à créer :**
1. view/login/form.jsp,
2. view/home/index.php 
3. view/dashboard/dashboard.jsp
4. view/admin/ingredients/form.jsp
5. view/admin/produit/form.jsp
6. view/admin/recette_de_base/form.jsp
7. view/admin/itineraire/form.jsp

* **Travail à faire :** Intégrer tout le backend dans ces views
- view/home/index.php  représente la page principale de notre application(pseudo plateforme)
**Remarques :** En cas d'ajout de nouveaux fichiers veuillez informer les CP*

* **Durée totale estimée :** 3h

* **DEADLINE :** Dimanche 20h
---
